import { GetWidgetSjrPipe } from './get-widget-sjr.pipe';

describe('GetWidgetSjrPipe', () => {
  it('create an instance', () => {
    const pipe = new GetWidgetSjrPipe();
    expect(pipe).toBeTruthy();
  });
});
